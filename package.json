{
  "name": "@johntalton/tic",
  "type": "module",
  "scripts": {
    "start": "./node_modules/.bin/http-server --robots --cors -b -g -S -C ./certificates/localhost-cert.pem -K ./certificates/localhost-privkey.pem ./public",
    "serve": "node --env-file=.env src/index.js"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  },
  "dependencies": {
    "@johntalton/sse-util": "^0.0.4"
  }
}
