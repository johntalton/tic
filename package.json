{
  "name": "@johntalton/tic",
  "type": "module",
  "scripts": {
    "start": "./node_modules/.bin/http-server --cors -b -g -S -C ./certificates/localhost-cert.pem -K ./certificates/localhost-privkey.pem ./public",
    "serve": "node --permission --allow-fs-read=. --allow-net --env-file=.env src/index.js"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  },
  "dependencies": {
    "@johntalton/sse-util": "^0.0.4"
  }
}
